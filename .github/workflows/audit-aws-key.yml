name: Audit AWS Access Key

on:
  workflow_dispatch:
    inputs:
      access_key_id:
        description: "AWS Access Key ID (AKIA...)"
        required: true
      days:
        description: "Look back window in days"
        default: "14"
        required: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  audit:
    name: Audit Key Usage
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # v6.0.0
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Run audit
        id: audit
        env:
          KEY_ID: ${{ inputs.access_key_id }}
          DAYS: ${{ inputs.days }}
        run: |
          DAYS_VALUE="${DAYS:-14}"
          bash scripts/audit-aws-key.sh "$KEY_ID" --days "$DAYS_VALUE" | tee audit.out
          echo "audit_log<<EOF" >> $GITHUB_OUTPUT
          sed -n '1,200p' audit.out >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Summary
        run: |
          DAYS_VALUE="${{ inputs.days }}"
          if [ -z "$DAYS_VALUE" ]; then
            DAYS_VALUE="14"
          fi
          {
            echo "### AWS Access Key Audit"
            echo "- Access key ID was provided at manual run time"
            echo "- Window: ${DAYS_VALUE} days"
            echo
            echo '```'
            sed -n '1,200p' audit.out
            echo '```'
          } >> $GITHUB_STEP_SUMMARY
