---
alwaysApply: true
---

## Shipping (early-stage)

Canonical detail: **`AGENTS.md`**. This file is the Cursor-sized summary.

1. Finish work on **`main`** in the same session; no parking completed work on side branches.
2. Production deploy path: **`.github/workflows/deploy-ssm.yml`** only.
3. After each push to `main`: confirm the deploy workflow succeeded and **`https://practica.jpagan.com/health/`** returns **200**.
4. Failed deploy: **fix-forward** on `main` immediately.

**Local minimum before push:** ruff + core-loop when backend behavior changes; `npm run build` when the frontend changes (see `.github/workflows/`).

**If release strategy changes** (e.g. staging gates, PR-only): update **`AGENTS.md`**, **`.cursor/rules.yaml`**, and **this file** in **one** commit.
