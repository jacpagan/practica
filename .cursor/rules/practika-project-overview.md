# Practica — pointer (not a second spec)

**Use instead:** `AGENTS.md` (commands, structure, conventions), `docs/practica-v2-prd.md` (product), `docs/platform-effects-mvp-playbook.md` (shipped behavior).

This file used to duplicate an outdated “Practika” architecture sketch. **Do not** treat it as current layout or naming; the repo is **Practica** with `apps/backend/` + `apps/frontend/` as in `AGENTS.md`.
